# Backend Stocker — Node / Express / Sequelize
# ─────────────────────────────────────────────

node_modules/
*.log
npm-debug.log*

# Env / secrets — CRÍTICO no subir
.env
.env.local
.env.*.local
a.sql
# Respaldos que deja scripts/arca-cert.js: contienen el .env entero.
.env.bak-*
.env.bak
# Certificados y claves sueltos, por si quedan al pie del proyecto
*.key
*.crt
*.pem
*.pfx   
*.p12
*.csr

# Storage — PDFs generados, certificados ARCA, assets
storage/
backend/storage/

# Editor / IDE
.vscode/
.idea/
.claude/
*.swp

# OS
.DS_Store
Thumbs.db

# Carpetas del monorepo que NO van en esta branch (por si aparecen restos locales)
backend/
frontend/

# Legacy sueltos
{backend/
isumayorista/
stocker/
backisu_project/
Ventoy/
